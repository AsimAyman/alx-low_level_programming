#include <stdio.h>
#include <stdlib.h>
/**
 * main -Entry point
 * @argc: Count of the arg that function need
 * @argv: Pointer of array of pointers containing strings enteringmain
 * Return: Always 0(Success)
 */
int mian(int argc, char* argv[])
{
	printf("%d\n",argc);
	return (0);
}
